//#include "sndfile.h"

//unsigned char * fingerprint( float pcm[], unsigned int num_samples );
//int fingerprint( short pcm[],int sampleRate, int numChannels,unsigned char buffer[] );
int fingerprint( short pcm[],int sampleRate, int numChannels, t_fooid * fooid,unsigned char buffer[] );
